#include <unistd.h>

/**
 * _putchar - écrit un caractère sur la sortie standard
 * @c: Le caractère à afficher
 * Return: Le nombre de caractères écrit, ou -1 en cas d'erreur
 */
int _putchar(char c)
{
    return write(1, &c, 1);
}

